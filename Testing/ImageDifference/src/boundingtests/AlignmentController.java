package boundingtests;

import experiments.FastPDF;

import java.awt.image.BufferedImage;

public class AlignmentController {
    public static BufferedImage rotatePlusAlign(BufferedImage original, BufferedImage scan) {
        //BufferedImage rotationCorrected = FastPDF.rotationCorrect(scan);
        //BufferedImage translationScaleRotCorrected = PDFWrapper.align(original, rotationCorrected);
        //return translationScaleRotCorrected;
        return null;
    }
}
